name: Test CI
on:
  push:
    branches-ignore:
      - 'master'
      - '_bors*'
    paths:
      - 'testFolder2/**'

jobs:
  cargo-format2:
    runs-on: ubuntu-20.04
    timeout-minutes: 5
    steps:
      - run: echo Hola2
